### Memory Management Commands

Source: https://github.com/openclaw/openclaw/blob/main/docs/cli/index.md

Commands for managing and querying memory, including status, indexing, and semantic search.

```APIDOC
## Memory Management Commands

### Description
Commands for managing and querying memory, including status, indexing, and semantic search over `MEMORY.md` and `memory/*.md` files.

### Commands
- `openclaw memory status`
  - **Description**: Display statistics about the memory index.
- `openclaw memory index`
  - **Description**: Re-index all memory files.
- `openclaw memory search "<query>"` or `openclaw memory --query "<query>"`
  - **Description**: Perform a semantic search over the indexed memory content.
```

--------------------------------

### Check System Memory Usage

Source: https://github.com/openclaw/openclaw/blob/main/docs/platforms/raspberry-pi.md

This command displays a human-readable summary of the system's memory usage, including total, used, free, shared, buffer, and cache memory. It is useful for diagnosing memory-related performance issues.

```bash
free -h
```

--------------------------------

### Memory Reflection CLI Command Example

Source: https://github.com/openclaw/openclaw/blob/main/docs/experiments/research/memory.md

Shows how to trigger the memory reflection process using the OpenClaw CLI. This command processes memory entries from the last 7 days.

```bash
openclaw memory reflect --since 7d
```

--------------------------------

### Retrieve Agent Memory States in SQL

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/sqlite.md

Fetches the memory states of all agents. This information is essential for restoring the context of subagents when resuming an interrupted run.

```sql
SELECT name, memory FROM agents;
```

--------------------------------

### Memory Recall CLI Command Example

Source: https://github.com/openclaw/openclaw/blob/main/docs/experiments/research/memory.md

Demonstrates how to use the OpenClaw CLI to recall memory entries. This command fetches the most recent 25 entries from the last 30 days.

```bash
openclaw memory recall "…" --k 25 --since 30d
```

--------------------------------

### SQL for Project-Scoped Agent Memory Management

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/postgres.md

This SQL demonstrates how to manage memory for project-scoped agents, where the `run_id` is NULL. It covers reading and updating the agent's memory at the project level.

```sql
-- Read project-scoped agent memory
SELECT memory FROM openprose.agents WHERE name = 'advisor' AND run_id IS NULL;

-- Update project-scoped agent memory
UPDATE openprose.agents SET memory = '...' WHERE name = 'advisor' AND run_id IS NULL;
```

--------------------------------

### OpenProse Persistent Agent Memory File Format

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/primitives/session.md

Defines the structure of the memory file used by persistent agents, including 'Current Understanding' and other accumulated knowledge.

```markdown
# Agent Memory: {name}

## Current Understanding

{Your accumulated knowledge about the project/task}


```

--------------------------------

### Enable Session Memory Search (JSON5)

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory.md

Enables experimental session memory indexing and search within OpenClaw. This configuration allows agents to index session transcripts and surface them via `memory_search`, with updates handled asynchronously.

```json5
agents: {
  defaults: {
    memorySearch: {
      experimental: { sessionMemory: true },
      sources: ["memory", "sessions"]
    }
  }
}
```

--------------------------------

### Manage Project-Scoped Persistent Memory

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/lib/README.md

The `project-memory.prose` utility offers project-scoped institutional memory, understanding project architecture and decisions. It recommends a `sqlite+` backend for persistence and takes a mode (ingest, query, update, summarize) and content as input.

```bash
prose run lib/project-memory.prose --backend sqlite+
# Inputs: mode (ingest|query|update|summarize), content
```

--------------------------------

### Persistent Agent Memory Management

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/prose.md

Details the process for persistent agents that use the 'resume:' functionality. It explains how agents should read their memory file for prior context and then update it with their compacted state after processing the task.

```text
Your memory is at:
.prose/runs/20260115-143052-a7b3c9/agents/captain/memory.md

Read it first to understand your prior context. When done, update it
with your compacted state following the guidelines in primitives/session.md.

```
```

--------------------------------

### Manage User-Scoped Persistent Memory

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/lib/README.md

The `user-memory.prose` program provides cross-project persistent personal memory, learning user preferences and patterns. It recommends using a `sqlite+` backend for durable storage and requires a mode (teach, query, reflect) and content as input.

```bash
prose run lib/user-memory.prose --backend sqlite+
# Inputs: mode (teach|query|reflect), content
```

--------------------------------

### Configure QMD Memory Backend Settings

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory.md

This configuration block sets up the QMD backend for memory storage, including session management, citation preferences, and indexing rules for Markdown files. It allows for customization of update intervals, result limits, and scope rules for memory access.

```json5
memory: {
  backend: "qmd",
  citations: "auto",
  qmd: {
    includeDefaultMemory: true,
    update: { interval: "5m", debounceMs: 15000 },
    limits: { maxResults: 6, timeoutMs: 4000 },
    scope: {
      default: "deny",
      rules: [
        { action: "allow", match: { chatType: "direct" } },
        // Normalized session-key prefix (strips `agent:<id>:`) .
        { action: "deny", match: { keyPrefix: "discord:channel:" } },
        // Raw session-key prefix (includes `agent:<id>:`) .
        { action: "deny", match: { rawKeyPrefix: "agent:main:discord:" } },
      ]
    },
    paths: [
      { name: "docs", path: "~/notes", pattern: "**/*.md" }
    ]
  }
}
```

--------------------------------

### Agent Memory File Structure

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/filesystem.md

Details the structure of agent memory files, including 'memory.md' for the agent's current state and historical invocation segments.

```markdown
# Agent Memory: captain

## Current Understanding

The project is implementing a REST API for user management.
Architecture uses Express + PostgreSQL. Test coverage target is 80%.

## Decisions Made

- 2026-01-15: Approved JWT over session tokens (simpler stateless auth)
- 2026-01-15: Set 80% coverage threshold (balances quality vs velocity)

## Open Concerns

- Rate limiting not yet implemented on login endpoint
- Need to verify OAuth flow works with new token format
```

```markdown
# Segment 001

timestamp: 2026-01-15T14:32:15Z
prompt: "Review the research findings"

## Summary

- Reviewed: docs from parallel research session
- Found: good coverage of core concepts, missing edge cases
- Decided: proceed with implementation, note gaps for later
- Next: review implementation against identified gaps
```

--------------------------------

### Memory Search Configuration

Source: https://github.com/openclaw/openclaw/blob/main/docs/gateway/configuration-examples.md

Defines settings for memory search functionality, including the provider, model for embeddings, remote API key, and additional paths to search for memory data.

```json
{
  "memorySearch": {
    "provider": "gemini",
    "model": "gemini-embedding-001",
    "remote": {
      "apiKey": "${GEMINI_API_KEY}"
    },
    "extraPaths": ["../team-docs", "/srv/shared-notes"]
  }
}
```

--------------------------------

### Manage System Memory (Bash)

Source: https://github.com/openclaw/openclaw/blob/main/docs/platforms/digitalocean.md

These bash commands assist in troubleshooting 'out of memory' errors. They allow checking current memory usage and provide guidance on adding swap space or upgrading system resources.

```bash
# Check memory
free -h

# Add more swap
# Or upgrade to $12/mo droplet (2GB RAM)
```

--------------------------------

### Persistent Agent Database Operations

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/sqlite.md

Shows common SQLite operations for persistent agents, including reading current state, updating memory, and recording segment information. It uses `sqlite3` commands to interact with the agent's memory and segments.

```shell
sqlite3 -json .prose/runs/20260116-143052-a7b3c9/state.db "SELECT memory FROM agents WHERE name = 'captain'"
```

```shell
sqlite3 .prose/runs/20260116-143052-a7b3c9/state.db "UPDATE agents SET memory = '...', updated_at = datetime('now') WHERE name = 'captain'"
```

```shell
sqlite3 .prose/runs/20260116-143052-a7b3c9/state.db "INSERT INTO agent_segments (agent_name, segment_number, prompt, summary) VALUES ('captain', 3, '...', '...')"
```

--------------------------------

### SQL for Persistent Agent Memory Management

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/postgres.md

These SQL commands manage the memory of persistent agents within OpenCLaw. They include reading the agent's current state, updating it, and recording specific agent segments with prompts and summaries.

```sql
-- Read your current state:
  psql "$OPENPROSE_POSTGRES_URL" -t -A -c "SELECT memory FROM openprose.agents WHERE name = 'captain' AND run_id = '20260116-143052-a7b3c9'"

-- Update when done:
  psql "$OPENPROSE_POSTGRES_URL" -c "UPDATE openprose.agents SET memory = '...', updated_at = NOW() WHERE name = 'captain' AND run_id = '20260116-143052-a7b3c9'"

-- Record this segment:
  psql "$OPENPROSE_POSTGRES_URL" -c "INSERT INTO openprose.agent_segments (agent_name, run_id, segment_number, prompt, summary) VALUES ('captain', '20260116-143052-a7b3c9', 3, '...', '...')"
```

--------------------------------

### Agent Persistence Configuration

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/compiler.md

Demonstrates how to configure agent memory persistence. It shows execution-scoped, project-scoped, and custom path persistence options.

```prose
# Execution-scoped persistence (memory dies with run) agent captain:
  model: opus
  persist: true
  prompt: "You coordinate and review"

# Project-scoped persistence (memory survives across runs) agent advisor:
  model: opus
  persist: project
  prompt: "You provide architectural guidance"

# Custom path persistence agent shared:
  model: opus
  persist: ".prose/custom/shared-agent/"
  prompt: "Shared across programs"
```

--------------------------------

### Disable Session Memory Hook (JSON)

Source: https://github.com/openclaw/openclaw/blob/main/src/hooks/bundled/session-memory/HOOK.md

Example JSON configuration to disable the session-memory hook. By setting 'enabled' to false, the hook will not be active.

```json
{
  "hooks": {
    "internal": {
      "entries": {
        "session-memory": {
          "enabled": false
        }
      }
    }
  }
}
```

--------------------------------

### Add Extra Paths for Memory Indexing

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory.md

This configuration allows specifying additional directories or files to be indexed by the memory search. It's useful for including Markdown content from outside the default workspace, ensuring comprehensive search results.

```json5
agents: {
  defaults: {
    memorySearch: {
      extraPaths: ["../team-docs", "/srv/shared-notes/overview.md"]
    }
  }
}
```

--------------------------------

### SQLite FTS5 for Lexical Search

Source: https://github.com/openclaw/openclaw/blob/main/docs/experiments/research/memory.md

Illustrates the use of SQLite's FTS5 extension for efficient lexical search within the memory index. This approach is recommended for initial implementation.

```sql
CREATE VIRTUAL TABLE memory_fts USING fts5(content, "rowid" UN ???????);
INSERT INTO memory_fts (rowid, content) SELECT rowid, content FROM memory_table;
```

--------------------------------

### Configure Embedding Cache (JSON5)

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory.md

Enables and configures the SQLite embedding cache for OpenClaw's memory search. This feature improves performance by storing chunk embeddings, reducing the need for re-embedding unchanged text, especially beneficial for frequently updated sources like session transcripts.

```json5
agents: {
  defaults: {
    memorySearch: {
      cache: {
        enabled: true,
        maxEntries: 50000
      }
    }
  }
}
```

--------------------------------

### Temporal Decay Formula

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory.md

Calculates a decayed score based on the age of a result, applying an exponential multiplier. The decay rate is determined by a half-life, where the score halves after a specified number of days. This ensures recent memories naturally rank higher.

```mathematica
decayedScore = score × e^(-? × ageInDays)
where ? = ln(2) / halfLifeDays
```

--------------------------------

### Reduce GPU Memory Allocation for Headless Operation

Source: https://github.com/openclaw/openclaw/blob/main/docs/platforms/raspberry-pi.md

This command reduces the GPU memory allocation to 16MB, which is suitable for headless Raspberry Pi operations where graphical output is not required. It modifies the `/boot/config.txt` file to free up system RAM for other processes.

```bash
echo 'gpu_mem=16' | sudo tee -a /boot/config.txt
```

--------------------------------

### Configure Session Memory Hook (JSON)

Source: https://github.com/openclaw/openclaw/blob/main/src/hooks/bundled/session-memory/HOOK.md

Example JSON configuration to enable and customize the session-memory hook. This allows setting the number of messages to include in the saved session context.

```json
{
  "hooks": {
    "internal": {
      "entries": {
        "session-memory": {
          "enabled": true,
          "messages": 25
        }
      }
    }
  }
}
```

--------------------------------

### Monitor System Memory Usage

Source: https://github.com/openclaw/openclaw/blob/main/docs/platforms/digitalocean.md

Provides commands to monitor the memory usage on the DigitalOcean droplet. `free -h` shows a human-readable overview of memory and swap usage, while `htop` provides an interactive process viewer.

```bash
free -h
htop
```

--------------------------------

### Configure Hybrid Memory Search (JSON5)

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory.md

Enables and configures hybrid search, including temporal decay and MMR, within the OpenClaw agent defaults. This JSON5 configuration allows fine-tuning the balance between vector and text search, result diversity, and recency.

```json5
agents: {
  defaults: {
    memorySearch: {
      query: {
        hybrid: {
          enabled: true,
          vectorWeight: 0.7,
          textWeight: 0.3,
          candidateMultiplier: 4,
          mmr: {
            enabled: true,    // default: false
            lambda: 0.7       // 0 = max diversity, 1 = max relevance
          },
          temporalDecay: {
            enabled: true,    // default: false
            halfLifeDays: 30  // score halves every 30 days
          }
        }
      }
    }
  }
}
```

--------------------------------

### Disable Session Memory Hook (Bash)

Source: https://github.com/openclaw/openclaw/blob/main/src/hooks/bundled/session-memory/HOOK.md

Command-line instruction to disable the session-memory hook using the OpenClaw CLI. This is a quick way to turn off the hook's functionality.

```bash
openclaw hooks disable session-memory
```

--------------------------------

### Basic Opinion Structure

Source: https://github.com/openclaw/openclaw/blob/main/docs/experiments/research/memory.md

Defines the structure for storing opinions within the memory system. Each opinion includes the statement, confidence level, update timestamp, and links to supporting/contradicting evidence.

```json
{
  "statement": "Peter prefers concise replies on WhatsApp.",
  "confidence": 0.95,
  "last_updated": "2025-11-27T10:00:00Z",
  "evidence": {
    "supporting": ["fact_id_1", "fact_id_2"],
    "contradicting": []
  }
}
```

--------------------------------

### Configure Automatic Memory Flush in OpenClaw

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory.md

This JSON5 configuration snippet shows how to enable and tune the automatic memory flush feature in OpenClaw. It controls when the flush is triggered based on token thresholds and defines the system and user prompts used during the flush.

```json5
{
  agents: {
    defaults: {
      compaction: {
        reserveTokensFloor: 20000,
        memoryFlush: {
          enabled: true,
          softThresholdTokens: 4000,
          systemPrompt: "Session nearing compaction. Store durable memories now.",
          prompt: "Write any lasting notes to memory/YYYY-MM-DD.md; reply with NO_REPLY if nothing to store.",
        },
      },
    },
  },
}
```

--------------------------------

### Configure Memory Flush Settings

Source: https://github.com/openclaw/openclaw/blob/main/docs/reference/session-management-compaction.md

Shows the configuration options for OpenClaw's pre-compaction memory flush feature. This includes enabling the flush, setting a soft threshold for triggering it, and defining prompts for the silent flush turn.

```yaml
agents.defaults.compaction.memoryFlush:
  enabled: true
  softThresholdTokens: 4000
  prompt: "Write memory now. NO_REPLY"
  systemPrompt: "Save current session state to disk. NO_REPLY"
```

--------------------------------

### Define Resume Statement in Prose

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/compiler.md

Defines a `resume` statement to continue a persistent agent, loading its accumulated memory. This allows for context-aware continuation of conversations.

```prose
resume: agentName
  prompt: "Continue from where we left off"
```

--------------------------------

### Configure Local Embeddings

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory.md

This configuration sets the memory search provider to use a local embedding model. You need to specify the path to the model file (GGUF or `hf:` URI). Optionally, you can disable remote fallback by setting `memorySearch.fallback` to 'none'.

```json5
agents: {
  defaults: {
    memorySearch: {
      provider: "local",
      local: {
        modelPath: "path/to/your/model.gguf"
      },
      fallback: "none"
    }
  }
}
```

--------------------------------

### Configure Session Sync Thresholds (JSON5)

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory.md

Sets the delta thresholds for synchronizing session memory indexing in OpenClaw. These defaults determine when session updates are batched for asynchronous indexing, balancing responsiveness with indexing load.

```json5
agents: {
  defaults: {
    memorySearch: {
      sync: {
        sessions: {
          deltaBytes: 100000,   // ~100 KB
          deltaMessages: 50     // JSONL lines
        }
      }
    }
  }
}
```

--------------------------------

### Resume Persistent Agent Session

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/prose.md

Shows how to resume a persistent agent's session, emphasizing that the agent's memory file content and output path are included in the prompt for subsequent invocations.

```prose
agent captain:
  model: opus
  persist: true
  prompt: "You coordinate and review"

# First invocation
session: captain
  prompt: "Review the plan"

# Subsequent invocation - loads memory
resume: captain
  prompt: "Review step 1"
```

--------------------------------

### Configure Gemini Embeddings (Native)

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory.md

This configuration sets the memory search provider to use the Gemini embeddings API directly. It specifies the model and includes a placeholder for your Gemini API key. The `remote.baseUrl` is optional and defaults to the Gemini API base URL.

```json5
agents: {
  defaults: {
    memorySearch: {
      provider: "gemini",
      model: "gemini-embedding-001",
      remote: {
        apiKey: "YOUR_GEMINI_API_KEY"
      }
    }
  }
}
```

--------------------------------

### Configure SQLite Vector Acceleration in OpenClaw

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory.md

Enables the sqlite-vec extension for fast vector distance queries within a SQLite virtual table. If the extension is missing or fails, OpenClaw falls back to in-process cosine similarity. The extensionPath can override the default bundled path.

```json5
agents: {
  defaults: {
    memorySearch: {
      store: {
        vector: {
          enabled: true,
          extensionPath: "/path/to/sqlite-vec"
        }
      }
    }
  }
}
```

--------------------------------

### Sanitize Session History (TypeScript)

Source: https://github.com/openclaw/openclaw/blob/main/docs/reference/transcript-hygiene.md

Applies various sanitization and repair rules to the session history in memory. This function centralizes the application of transcript hygiene steps before building model context. It incorporates logic for image sanitization and tool call input validation.

```TypeScript
import { sanitizeSessionHistory } from "src/agents/pi-embedded-runner/google.ts";

// Example usage:
// sanitizeSessionHistory(session);

```

--------------------------------

### Run Vitest Tests on Memory-Constrained Hosts

Source: https://github.com/openclaw/openclaw/blob/main/docs/reference/test.md

Configures Vitest to run with specific profiles optimized for memory-constrained environments. It also serializes gateway tests to prevent potential memory issues.

```bash
OPENCLAW_TEST_PROFILE=low OPENCLAW_TEST_SERIAL_GATEWAY=1 pnpm test
```

--------------------------------

### Directory Structure for File-System State Management

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/filesystem.md

Illustrates the hierarchical directory structure used by OpenProse for managing execution state, including configuration, run-specific data, and agent memory.

```shell
# Project-level state (in working directory)
.prose/
+-- .env                              # Config (simple key=value format)
+-- runs/
¦   +-- {YYYYMMDD}-{HHMMSS}-{random}/
¦       +-- program.prose             # Copy of running program
¦       +-- state.md                  # Execution state with code snippets
¦       +-- bindings/
¦       ¦   +-- {name}.md             # Root scope bindings
¦       ¦   +-- {name}__{execution_id}.md  # Scoped bindings (block invocations)
¦       +-- imports/
¦       ¦   +-- {handle}--{slug}/     # Nested program executions (same structure recursively)
¦       +-- agents/
¦           +-- {name}/
¦               +-- memory.md         # Agent's current state
¦               +-- {name}-001.md     # Historical segments (flattened)
¦               +-- {name}-002.md
¦               +-- ...
+-- agents/                           # Project-scoped agent memory
    +-- {name}/
        +-- memory.md
        +-- {name}-001.md
        +-- ...

# User-level state (in home directory)
~/.prose/
+-- agents/                           # User-scoped agent memory (cross-project)
    +-- {name}/
        +-- memory.md
        +-- {name}-001.md
        +-- ...

```

--------------------------------

### Enable Batch Indexing with OpenAI or Gemini

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory.md

This configuration enables batch indexing for large-corpus indexing when using OpenAI or Gemini providers. It allows for more efficient and cost-effective indexing by submitting multiple embedding requests in a single batch job. You can tune batch parameters like concurrency and wait times.

```json5
agents: {
  defaults: {
    memorySearch: {
      provider: "openai",
      model: "text-embedding-3-small",
      fallback: "openai",
      remote: {
        batch: { enabled: true, concurrency: 2 }
      },
      sync: { watch: true }
    }
  }
}
```

--------------------------------

### Configure QMD Backend in OpenClaw

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory.md

Example of how to enable the QMD backend by setting the memory.backend configuration option in OpenClaw.

```ini
memory.backend = "qmd"
```

--------------------------------

### Context Serialization Strategy

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/in-context.md

Outlines strategies for serializing context based on its size, ranging from verbatim passing for small contexts to extracting essentials for very large ones. It also provides the format for context provision.

```text
Context provided:
---
research: "Key findings about AI safety..."
analysis: "Risk assessment shows..."
---
```

--------------------------------

### Configure Custom OpenAI-Compatible Endpoint for Embeddings

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory.md

Integrates OpenClaw with a custom OpenAI-compatible API for generating embeddings. The `remote` configuration takes precedence over standard OpenAI settings. Headers can be merged or overridden.

```json5
agents: {
  defaults: {
    memorySearch: {
      provider: "openai",
      model: "text-embedding-3-small",
      remote: {
        baseUrl: "https://api.example.com/v1/",
        apiKey: "YOUR_REMOTE_API_KEY",
        headers: {
          "X-Organization": "org-id",
          "X-Project": "project-id"
        }
      }
    }
  }
}
```

--------------------------------

### Store Large Output Values in SQL Attachments

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/sqlite.md

Demonstrates inserting a binding into the database where the primary value is too large for direct storage. The content is written to a file, and only the file path is stored in the `attachment_path` column.

```sql
INSERT INTO bindings (name, kind, value, attachment_path, source_statement)
VALUES (
  'full_report',
  'let',
  'Full analysis report (847KB) - see attachment',
  'attachments/full_report.md',
  'let full_report = session "Generate comprehensive report"'
);
```

--------------------------------

### SQL: Define Core OpenCLaw Schema Tables

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/sqlite.md

SQL statements to create the essential tables for the OpenCLaw VM, including run metadata, execution history, variable bindings, agent memory, agent invocation logs, and import registry. This schema is designed to be extensible.

```sql
CREATE TABLE IF NOT EXISTS run (
    id TEXT PRIMARY KEY,
    program_path TEXT,
    program_source TEXT,
    started_at TEXT DEFAULT (datetime('now')),
    updated_at TEXT DEFAULT (datetime('now')),
    status TEXT DEFAULT 'running',  -- running, completed, failed, interrupted
    state_mode TEXT DEFAULT 'sqlite'
);

-- Execution position and history
CREATE TABLE IF NOT EXISTS execution (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    statement_index INTEGER,
    statement_text TEXT,
    status TEXT,  -- pending, executing, completed, failed, skipped
    started_at TEXT,
    completed_at TEXT,
    error_message TEXT,
    parent_id INTEGER REFERENCES execution(id),  -- for nested blocks
    metadata TEXT  -- JSON for construct-specific data (loop iteration, parallel branch, etc.)
);

-- All named values (input, output, let, const)
CREATE TABLE IF NOT EXISTS bindings (
    name TEXT,
    execution_id INTEGER,  -- NULL for root scope, non-null for block invocations
    kind TEXT,  -- input, output, let, const
    value TEXT,
    source_statement TEXT,
    created_at TEXT DEFAULT (datetime('now')),
    updated_at TEXT DEFAULT (datetime('now')),
    attachment_path TEXT,  -- if value is too large, store path to file
    PRIMARY KEY (name, IFNULL(execution_id, -1))  -- IFNULL handles NULL for root scope
);

-- Persistent agent memory
CREATE TABLE IF NOT EXISTS agents (
    name TEXT PRIMARY KEY,
    scope TEXT,  -- execution, project, user, custom
    memory TEXT,
    created_at TEXT DEFAULT (datetime('now')),
    updated_at TEXT DEFAULT (datetime('now'))
);

-- Agent invocation history
CREATE TABLE IF NOT EXISTS agent_segments (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    agent_name TEXT REFERENCES agents(name),
    segment_number INTEGER,
    timestamp TEXT DEFAULT (datetime('now')),
    prompt TEXT,
    summary TEXT,
    UNIQUE(agent_name, segment_number)
);

-- Import registry
CREATE TABLE IF NOT EXISTS imports (
    alias TEXT PRIMARY KEY,
    source_url TEXT,
    fetched_at TEXT,
    inputs_schema TEXT,  -- JSON
    outputs_schema TEXT  -- JSON
);

```

--------------------------------

### Context Passing in Prose

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/prose.md

Shows how variables are used to capture session outputs and pass them to subsequent sessions in Prose. It covers different forms of context passing, including single variables, arrays, and named objects.

```prose
let research = session "Research the topic"

session "Write summary"
  context: research
```

--------------------------------

### Sequential Session Execution in Prose

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/compiler.md

Illustrates how multiple sessions are executed one after another. Each session must complete before the next one begins.

```prose
session "First task"
session "Second task"
session "Third task"
```

--------------------------------

### VM Loop Tracking - Initialization and Iteration Update

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/sqlite.md

SQL examples for tracking loop execution. The first statement shows the initial insertion of an execution record with loop metadata, including max iterations and condition. The second updates the current iteration count.

```sql
-- Loop metadata tracks iteration state
INSERT INTO execution (statement_index, statement_text, status, metadata)
VALUES (10, 'loop until **analysis complete** (max: 5):', 'executing',
  '{"loop_id": "l1", "max_iterations": 5, "current_iteration": 0, "condition": "**analysis complete**"}');

-- Update iteration
UPDATE execution
SET metadata = json_set(metadata, '$.current_iteration', 2),
    updated_at = datetime('now')
WHERE json_extract(metadata, '$.loop_id') = 'l1';
```

--------------------------------

### Implement Fixed Loops in Prose

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/prose.md

Shows how to implement fixed-count loops in Prose. The 'repeat' keyword executes a block a specified number of times sequentially, while the 'for' loop iterates over a collection, binding each item to a variable.

```prose
repeat 3:
  session "Generate idea"
```

```prose
for topic in ["AI", "ML", "DL"]:
  session "Research"
    context: topic
```

--------------------------------

### Subagent Database Interaction (Root Scope)

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/sqlite.md

Demonstrates how the VM provides the database path and instructions for inserting data into the bindings table from the root scope. It uses SQLite to insert or replace a record with details about a 'research' binding.

```shell
sqlite3 .prose/runs/20260116-143052-a7b3c9/state.db "
  INSERT OR REPLACE INTO bindings (name, execution_id, kind, value, source_statement, updated_at)
  VALUES (
    'research',
    NULL,  -- root scope
    'let',
    'AI safety research covers alignment, robustness...', 
    'let research = session: researcher',
    datetime('now')
  )
"
```

--------------------------------

### VM Conversation Narration Example

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/postgres.md

Illustrates how the Virtual Machine (VM) narrates key conversational events, even when using a database for persistence. This narration serves as the VM's 'mental model' of execution.

```text
[Position] Statement 3: let research = session: researcher
   Spawning session, will write to state database
   [Task tool call]
[Success] Session complete, binding written to DB
[Binding] research = <stored in openprose.bindings>
```

--------------------------------

### Implement Parallel For-Each Loop in Prose

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/prose.md

Demonstrates the 'parallel for' loop construct in Prose. This allows for concurrent execution of loop iterations, with all iterations starting simultaneously and the program waiting for all to complete.

```prose
parallel for item in items:
  session "Process"
    context: item
```

--------------------------------

### Bash: Read Binding Value from SQLite

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/sqlite.md

Bash command to query a SQLite database and retrieve the 'value' of a specific binding by its 'name'. The '-json' flag formats the output as JSON.

```bash
sqlite3 -json .prose/runs/20260116-143052-a7b3c9/state.db "
  SELECT value FROM bindings WHERE name = 'research'
"
```

--------------------------------

### OpenProse VM Completion Signals

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/primitives/session.md

Signals indicating the completion of a segment, detailing memory updates, and specifying the next steps for the VM.

```prose
SEGMENT COMPLETE
MEMORY UPDATES:
- [what to add to Current Understanding]
- [decisions to record]
- [concerns to track]
READY FOR: [what should happen next]
```

--------------------------------

### VM Conversation Narration Example

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/sqlite.md

Provides an example of how the VM narrates key events during execution, including statement processing, spawning sessions, task tool calls, and binding updates. This serves as the VM's 'mental model' of execution.

```text
[Position] Statement 3: let research = session: researcher
   Spawning session, will write to state.db
   [Task tool call]
[Success] Session complete, binding written to DB
[Binding] research = <stored in state.db>
```

--------------------------------

### Install QMD CLI

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory.md

Instructions for installing the QMD CLI using Bun. This is a prerequisite for enabling the QMD backend in OpenClaw.

```bash
bun install -g https://github.com/tobi/qmd
```

--------------------------------

### Subagent Database Interaction (Scoped Block)

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/sqlite.md

Illustrates how the VM provides database path and instructions for inserting data into the bindings table within a scoped block, including the execution_id. It uses SQLite to insert or replace a record for a 'result' binding.

```shell
sqlite3 .prose/runs/20260116-143052-a7b3c9/state.db "
  INSERT OR REPLACE INTO bindings (name, execution_id, kind, value, source_statement, updated_at)
  VALUES (
    'result',
    43,  -- scoped to this execution
    'let',
    'Processed chunk into 3 sub-parts...', 
    'let result = session "Process chunk"',
    datetime('now')
  )
"
```

--------------------------------

### Configure Default Agent Workspace

Source: https://github.com/openclaw/openclaw/blob/main/docs/help/faq.md

Sets the default workspace directory for agents. This configuration is typically placed in the OpenClaw configuration file. It ensures agents use a consistent location for their files and memory.

```json5
{
  agents: {
    defaults: {
      workspace: "~/.openclaw/workspace"
    }
  }
}
```

--------------------------------

### Capture Parallel Results into Variables

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/compiler.md

Demonstrates how to assign the results of parallel execution branches to named variables for later use. This allows for capturing and referencing outputs from concurrent tasks.

```prose
parallel:
  security = session "Security review"
  perf = session "Performance review"
  style = session "Style review"
```

--------------------------------

### Add Swap Space to Droplet

Source: https://github.com/openclaw/openclaw/blob/main/docs/platforms/digitalocean.md

Increases the available memory on the DigitalOcean droplet by creating and enabling a swap file. This is a recommended optimization for the 1GB RAM droplet to prevent Out-of-Memory errors.

```bash
fallocate -l 2G /swapfile
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile
echo '/swapfile none swap sw 0 0' >> /etc/fstab
```

--------------------------------

### Discretion Condition Evaluation in Prose

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/prose.md

Demonstrates how discretion markers (**) are used to define AI-evaluated conditions within Prose. It explains the evaluation approach, emphasizing context awareness, semantic interpretation, conservative judgment, and progress detection.

```prose
loop until **the code is bug-free**:
  session "Find and fix bugs"
```

--------------------------------

### Initialize Git Repository for Workspace (Bash)

Source: https://github.com/openclaw/openclaw/blob/main/docs/reference/AGENTS.default.md

Commands to initialize a Git repository within the OpenClaw workspace for version control and backup. This helps in tracking changes to agent files and memory.

```bash
cd ~/.openclaw/workspace
git init
git add AGENTS.md
git commit -m "Add Clawd workspace"
```

--------------------------------

### Complete Execution Trace Example (Prose)

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/in-context.md

Provides a detailed example of a complete program execution trace written in Prose. It includes agent definitions, session calls, parallel execution, loops, and context serialization, along with narration of each step.

```prose
agent researcher:
  model: sonnet

let research = session: researcher
  prompt: "Research AI safety"

parallel:
  a = session "Analyze risk A"
  b = session "Analyze risk B"

loop until **analysis complete** (max: 3):
  session "Synthesize"
    context: { a, b, research }
```

--------------------------------

### Scoped Binding Narration

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/in-context.md

Shows how bindings are annotated with their execution scope and how variable resolution occurs across different scopes. This is crucial for understanding data context in nested function calls.

```text
[Binding] let result = "computed value" (execution_id: 43)

[Binding] Resolving 'config': found in execution_id 41 (parent scope)
```

--------------------------------

### Combining Parallel Join Strategies and Failure Policies

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/compiler.md

Demonstrates how to combine different join strategies (like 'first' or 'any') with failure policies (like 'continue' or 'ignore') to create complex parallel execution behaviors. This allows for fine-grained control over how parallel tasks are managed.

```prose
# Race with resilience
parallel ("first", on-fail: "continue"):
  session "Fast but unreliable"
  session "Slow but reliable"

# Get any 2 results, ignoring failures
parallel ("any", count: 2, on-fail: "ignore"):
  session "Approach 1"
  session "Approach 2"
  session "Approach 3"
  session "Approach 4"
```

--------------------------------

### Build and Launch OpenClaw Docker Compose Services

Source: https://github.com/openclaw/openclaw/blob/main/docs/install/gcp.md

Commands to build the Docker images defined in `docker-compose.yml` and then start the `openclaw-gateway` service in detached mode. It also includes a troubleshooting tip for build failures due to insufficient memory.

```bash
docker compose build
docker compose up -d openclaw-gateway
```

--------------------------------

### Context Minimization: Pass Only Relevant Data (Prose)

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/guidance/patterns.md

This pattern emphasizes reducing processing costs and improving efficiency by passing only the necessary context to AI models. It contrasts a 'bad' example of passing excessive data with a 'good' example that extracts and passes only key findings.

```prose
# Bad: Passing everything
session "Write executive summary"
  context: [raw_data, analysis, methodology, appendices, references]

# Good: Pass only what's needed
let key_findings = session "Extract key findings from analysis"
  context: analysis

session "Write executive summary"
  context: key_findings
```

--------------------------------

### Migrate OpenClaw Setup to a New Machine

Source: https://github.com/openclaw/openclaw/blob/main/docs/help/faq.md

Steps to migrate an existing OpenClaw setup to a new machine by copying the state directory and workspace, then running `openclaw doctor`. This preserves bot memory, session history, auth, and channel state.

```bash
# On the new machine:
# 1. Install OpenClaw
# 2. Copy $OPENCLAW_STATE_DIR (default: ~/.openclaw) from the old machine.
# 3. Copy your workspace (default: ~/.openclaw/workspace) from the old machine.
# 4. Run openclaw doctor and restart the Gateway service.
```

--------------------------------

### Live System Resource Monitoring with htop

Source: https://github.com/openclaw/openclaw/blob/main/docs/platforms/raspberry-pi.md

This command launches `htop`, an interactive process viewer for Unix-like systems. It provides a real-time, color-coded overview of running processes, CPU usage, memory usage, and system load, allowing for dynamic performance monitoring.

```bash
htop
```

--------------------------------

### Bash: Query Parallel Branch Status in SQLite

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/sqlite.md

Bash command to query the 'execution' table in a SQLite database for records belonging to a specific parallel branch, identified by a 'parallel_id' in the 'metadata' JSON field. It retrieves the statement text and status.

```bash
sqlite3 .prose/runs/20260116-143052-a7b3c9/state.db "
  SELECT statement_text, status FROM execution
  WHERE json_extract(metadata, '$.parallel_id') = 'p1'
"
```

--------------------------------

### Execute Command in Background (OpenClaw Exec Tool)

Source: https://github.com/openclaw/openclaw/blob/main/docs/gateway/background-process.md

Initiates a command execution that can run in the background. The 'yieldMs' parameter determines the delay before auto-backgrounding, and 'background' forces immediate backgrounding. Output is managed in memory until polled.

```json
{
  "tool": "exec",
  "command": "sleep 5 && echo done",
  "yieldMs": 1000
}
```

```json
{
  "tool": "exec",
  "command": "npm run build",
  "background": true
}
```

--------------------------------

### Pass Multiple Contexts to Session

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/compiler.md

Multiple session outputs can be passed as context to a new session by providing an array of variable references.

```prose
let research = session "Research the topic"
let analysis = session "Analyze the findings"

session "Write final report"
  context: [research, analysis]
```

--------------------------------

### Configure OpenAI-Compatible Embeddings (Custom Endpoint)

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory.md

This configuration enables the use of a custom OpenAI-compatible endpoint for embeddings, such as OpenRouter or vLLM. It sets the provider to 'openai' and requires the `baseUrl` for the custom endpoint, along with an API key and optional custom headers.

```json5
agents: {
  defaults: {
    memorySearch: {
      provider: "openai",
      model: "text-embedding-3-small",
      remote: {
        baseUrl: "https://api.example.com/v1/",
        apiKey: "YOUR_OPENAI_COMPAT_API_KEY",
        headers: { "X-Custom-Header": "value" }
      }
    }
  }
}
```

--------------------------------

### Retry with Context and Backoff

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/compiler.md

Illustrates how retry and backoff properties can be used in conjunction with session context, showing a session that retrieves input data and then processes it with retry logic.

```prose
let data = session "Get input"
session "Process data"
  context: data
  retry: 3
  backoff: linear
```

--------------------------------

### Telegram Forum Topic Configuration Example (JSON5)

Source: https://github.com/openclaw/openclaw/blob/main/docs/channels/telegram.md

Illustrates how to configure per-topic agent routing in Telegram forum supergroups. Each topic can be assigned a specific agent by setting the `agentId` in the topic configuration. This provides isolated workspaces, memory, and sessions for each topic.

```json5
{
  channels: {
    telegram: {
      groups: {
        "-1001234567890": {
          topics: {
            "1": { agentId: "main" },      // General topic ? main agent
            "3": { agentId: "zu" },        // Dev topic ? zu agent
            "5": { agentId: "coder" }      // Code review ? coder agent
          }
        }
      }
    }
  }
}
```

--------------------------------

### Implement Unbounded Loops with Conditions in Prose

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/prose.md

Illustrates unbounded loops in Prose using the 'loop until' construct. The loop continues until a specified condition is met or a maximum iteration count is reached, providing control over execution.

```prose
loop until **task complete** (max: 10):
  session "Work on task"
```

--------------------------------

### Configure Local Embedding Auto-Download with node-llama-cpp

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory.md

Sets up OpenClaw to use local embeddings via node-llama-cpp. If the specified GGUF model is missing, it will be automatically downloaded to the cache. Requires a native build approval and may fall back to OpenAI if local setup fails.

```json5
agents: {
  defaults: {
    memorySearch: {
      provider: "local"
    }
  }
}
```

--------------------------------

### Heartbeat State Tracking Example (JSON)

Source: https://github.com/openclaw/openclaw/blob/main/docs/reference/templates/AGENTS.md

This JSON snippet demonstrates how to track the last checked timestamps for various heartbeat tasks. It includes fields for email, calendar, and weather checks, with null indicating a task has not yet been performed. This helps prevent redundant checks and manage the frequency of operations.

```json
{
  "lastChecks": {
    "email": 1703275200,
    "calendar": 1703260800,
    "weather": null
  }
}
```

--------------------------------

### Batching Similar Operations in Prose

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/guidance/patterns.md

Shows the 'batch-similar-work' pattern in Prose, contrasting an inefficient approach of many small sessions with an efficient one that groups similar operations. Batching amortizes overhead and improves performance by processing multiple items within a single session.

```prose
# Inefficient: Many small sessions
for file in files:
  session "Analyze {file}"

# Efficient: Batch analysis
session "Analyze all files and return structured findings for each"
  context: files
```

--------------------------------

### Importing Programs in Prose

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/prose.md

Demonstrates how to import and reference programs from a registry using the `use` statement in Prose. It shows the standard import path format and the use of aliases for referencing imported programs.

```prose
use "alice/research"
use "bob/critique" as critic
```

--------------------------------

### Manually Warm QMD Index and Download Models

Source: https://github.com/openclaw/openclaw/blob/main/docs/concepts/memory.md

Bash script to manually set environment variables for QMD's configuration and cache, update the index, embed data, and perform a test query. This helps pre-download models and warm the index for faster first searches.

```bash
# Pick the same state dir OpenClaw uses
STATE_DIR="${OPENCLAW_STATE_DIR:-$HOME/.openclaw}"

export XDG_CONFIG_HOME="$STATE_DIR/agents/main/qmd/xdg-config"
export XDG_CACHE_HOME="$STATE_DIR/agents/main/qmd/xdg-cache"

# (Optional) force an index refresh + embeddings
qmd update
qmd embed

# Warm up / trigger first-time model downloads
qmd query "test" -c memory-root --json >/dev/null 2>&1
```

--------------------------------

### SQL Queries for Resuming Execution

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/postgres.md

A collection of SQL queries designed to help resume an interrupted OpenClaw run. It includes statements to find the current execution position, retrieve all completed bindings, fetch agent memory states, and check the status of parallel execution blocks.

```sql
-- Find current position
SELECT statement_index, statement_text, status
FROM openprose.execution
WHERE run_id = '20260116-143052-a7b3c9' AND status = 'executing'
ORDER BY id DESC LIMIT 1;

-- Get all completed bindings
SELECT name, kind, value, attachment_path FROM openprose.bindings
WHERE run_id = '20260116-143052-a7b3c9';

-- Get agent memory states
SELECT name, scope, memory FROM openprose.agents
WHERE run_id = '20260116-143052-a7b3c9' OR run_id IS NULL;

-- Check parallel block status
SELECT metadata->>'branch' AS branch, status
FROM openprose.execution
WHERE run_id = '20260116-143052-a7b3c9'
  AND metadata->>'parallel_id' IS NOT NULL
  AND parent_id IS NOT NULL;
```

--------------------------------

### OpenProse SQL Schema Definition

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/postgres.md

Defines the core tables for the OpenProse VM, including 'run', 'execution', 'bindings', 'agents', 'agent_segments', and 'imports'. It also includes indexes for optimizing common queries. This schema manages execution state, data bindings, agent memory, and import registry.

```sql
-- Create dedicated schema for OpenProse state
CREATE SCHEMA IF NOT EXISTS openprose;

-- Run metadata
CREATE TABLE IF NOT EXISTS openprose.run (
    id TEXT PRIMARY KEY,
    program_path TEXT,
    program_source TEXT,
    started_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    status TEXT NOT NULL DEFAULT 'running'
        CHECK (status IN ('running', 'completed', 'failed', 'interrupted')),
    state_mode TEXT NOT NULL DEFAULT 'postgres',
    metadata JSONB DEFAULT '{}'::jsonb
);

-- Execution position and history
CREATE TABLE IF NOT EXISTS openprose.execution (
    id SERIAL PRIMARY KEY,
    run_id TEXT NOT NULL REFERENCES openprose.run(id) ON DELETE CASCADE,
    statement_index INTEGER NOT NULL,
    statement_text TEXT,
    status TEXT NOT NULL DEFAULT 'pending'
        CHECK (status IN ('pending', 'executing', 'completed', 'failed', 'skipped')),
    started_at TIMESTAMPTZ,
    completed_at TIMESTAMPTZ,
    error_message TEXT,
    parent_id INTEGER REFERENCES openprose.execution(id) ON DELETE CASCADE,
    metadata JSONB DEFAULT '{}'::jsonb
);

-- All named values (input, output, let, const)
CREATE TABLE IF NOT EXISTS openprose.bindings (
    name TEXT NOT NULL,
    run_id TEXT NOT NULL REFERENCES openprose.run(id) ON DELETE CASCADE,
    execution_id INTEGER,  -- NULL for root scope, non-null for block invocations
    kind TEXT NOT NULL CHECK (kind IN ('input', 'output', 'let', 'const')),
    value TEXT,
    source_statement TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    attachment_path TEXT,
    metadata JSONB DEFAULT '{}'::jsonb,
    PRIMARY KEY (name, run_id, COALESCE(execution_id, -1))  -- Composite key with scope
);

-- Persistent agent memory
CREATE TABLE IF NOT EXISTS openprose.agents (
    name TEXT NOT NULL,
    run_id TEXT,  -- NULL for project-scoped and user-scoped agents
    scope TEXT NOT NULL CHECK (scope IN ('execution', 'project', 'user', 'custom')),
    memory TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    metadata JSONB DEFAULT '{}'::jsonb,
    PRIMARY KEY (name, COALESCE(run_id, '__project__'))
);

-- Agent invocation history
CREATE TABLE IF NOT EXISTS openprose.agent_segments (
    id SERIAL PRIMARY KEY,
    agent_name TEXT NOT NULL,
    run_id TEXT,  -- NULL for project-scoped agents
    segment_number INTEGER NOT NULL,
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    prompt TEXT,
    summary TEXT,
    metadata JSONB DEFAULT '{}'::jsonb,
    UNIQUE (agent_name, COALESCE(run_id, '__project__'), segment_number)
);

-- Import registry
CREATE TABLE IF NOT EXISTS openprose.imports (
    alias TEXT NOT NULL,
    run_id TEXT NOT NULL REFERENCES openprose.run(id) ON DELETE CASCADE,
    source_url TEXT NOT NULL,
    fetched_at TIMESTAMPTZ,
    inputs_schema JSONB,
    outputs_schema JSONB,
    content_hash TEXT,
    metadata JSONB DEFAULT '{}'::jsonb,
    PRIMARY KEY (alias, run_id)
);

-- Indexes for common queries
CREATE INDEX IF NOT EXISTS idx_execution_run_id ON openprose.execution(run_id);
CREATE INDEX IF NOT EXISTS idx_execution_status ON openprose.execution(status);
CREATE INDEX IF NOT EXISTS idx_execution_parent_id ON openprose.execution(parent_id) WHERE parent_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_execution_metadata_gin ON openprose.execution USING GIN (metadata jsonb_path_ops);
CREATE INDEX IF NOT EXISTS idx_bindings_run_id ON openprose.bindings(run_id);
CREATE INDEX IF NOT EXISTS idx_bindings_execution_id ON openprose.bindings(execution_id) WHERE execution_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_agents_run_id ON openprose.agents(run_id) WHERE run_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_agents_project_scoped ON openprose.agents(name) WHERE run_id IS NULL;
CREATE INDEX IF NOT EXISTS idx_agent_segments_lookup ON openprose.agent_segments(agent_name, run_id);

```

--------------------------------

### VM Parallel Execution - Branch Completion and Check

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/sqlite.md

SQL statements showing how the VM marks a parallel branch as complete and how it checks if all branches have finished. This process relies on the `metadata` field to identify parallel execution instances and their branches.

```sql
-- VM marks branch complete after Task returns
UPDATE execution SET status = 'completed', completed_at = datetime('now')
WHERE json_extract(metadata, '$.parallel_id') = 'p1' AND json_extract(metadata, '$.branch') = 'a';

-- VM checks if all branches complete
SELECT COUNT(*) as pending FROM execution
WHERE json_extract(metadata, '$.parallel_id') = 'p1' AND status != 'completed';
```

--------------------------------

### Configure fly.toml for OpenClaw Deployment

Source: https://github.com/openclaw/openclaw/blob/main/docs/install/fly.md

This TOML configuration file defines the settings for deploying OpenClaw on Fly.io. It specifies the app name, build settings, environment variables, process configuration, HTTP service settings, VM size, and volume mounts for persistent storage. Key settings include binding to LAN, allowing unconfigured startup, matching internal port, recommended memory, and persisting state to `/data`.

```toml
app = "my-openclaw"  # Your app name
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"
  OPENCLAW_PREFER_PNPM = "1"
  OPENCLAW_STATE_DIR = "/data"
  NODE_OPTIONS = "--max-old-space-size=1536"

[processes]
  app = "node dist/index.js gateway --allow-unconfigured --port 3000 --bind lan"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

[[vm]]
  size = "shared-cpu-2x"
  memory = "2048mb"

[mounts]
  source = "openclaw_data"
  destination = "/data"
```

--------------------------------

### Agent Persistence and Resumption in OpenCLaw

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/compiler.md

Shows how to configure agents for persistence and how to use the `resume` statement to continue agent execution with its preserved state.

```OpenCLaw
agent PersistentAgent:
  persist: true

resume: PersistentAgent
```

--------------------------------

### Retrieve Completed Bindings in SQL

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/sqlite.md

Fetches all bindings that have been successfully completed, including their names, kinds, values, and attachment paths. This is part of the state needed to resume execution.

```sql
SELECT name, kind, value, attachment_path FROM bindings;
```

--------------------------------

### Destructure Program Outputs in Prose

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/prose.md

Demonstrates destructuring the output object returned from an invoked program in Prose. This allows direct assignment of specific outputs to variables for easier access.

```prose
let { findings, sources } = research(topic: "quantum computing")
```

--------------------------------

### Reassign Mutable Variable

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/compiler.md

Mutable variables declared with 'let' can be reassigned to update their value with new session results. Context can be passed to subsequent sessions.

```prose
let draft = session "Write initial draft"

# Revise the draft
draft = session "Improve the draft"
  context: draft
```

--------------------------------

### VM Parallel Execution - Start and Branch Creation

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/state/sqlite.md

SQL statements demonstrating how the VM marks the start of parallel execution and creates individual execution records for each branch. It utilizes the `metadata` JSON field to track parallel execution details.

```sql
-- VM marks parallel start
INSERT INTO execution (statement_index, statement_text, status, metadata)
VALUES (5, 'parallel:', 'executing', '{"parallel_id": "p1", "strategy": "all", "branches": ["a", "b", "c"]}');

-- VM creates execution record for each branch
INSERT INTO execution (statement_index, statement_text, status, parent_id, metadata)
VALUES (6, 'a = session "Task A"', 'executing', 5, '{"parallel_id": "p1", "branch": "a"}');
```

--------------------------------

### Manage Image Model Fallbacks

Source: https://github.com/openclaw/openclaw/blob/main/docs/cli/index.md

Manages fallback models for image generation. Allows listing, adding, removing, or clearing fallback image models.

```bash
openclaw models image-fallbacks add "dall-e-2"
openclaw models image-fallbacks list --plain
```

--------------------------------

### Nested Loop Example 2

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/compiler.md

Shows a fixed-iteration loop nested inside an unbounded loop, demonstrating flexibility in structuring iterative processes.

```prose
# Fixed inside unbounded
loop until **all batches processed** (max: 5):
  repeat 3:
    session "Process batch item"
```

--------------------------------

### List Stored Conversation Sessions

Source: https://github.com/openclaw/openclaw/blob/main/docs/cli/index.md

Lists stored conversation sessions with options for JSON output, verbose details, specifying a store path, and filtering by active minutes.

```bash
openclaw sessions [--json] [--verbose] [--store <path>] [--active <minutes>]
```

--------------------------------

### Start Session with Empty Context

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/compiler.md

Use an empty array `[]` for the 'context' property to start a session without any inherited context, ensuring a fresh start.

```prose
session "Independent task"
  context: []
```

--------------------------------

### Pass Single Context to Session

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/compiler.md

The 'context' property passes previous session outputs to a new session. A single variable can be passed as context.

```prose
let research = session "Research quantum computing"

session "Write summary"
  context: research
```

--------------------------------

### Assign Parallel Block to Variable

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/compiler.md

Explains how to assign an entire parallel block, including all its results, to a single variable. This allows treating the collection of parallel outputs as a single unit.

```prose
let results = parallel:
  session "Task A"
  session "Task B"
```

--------------------------------

### Minimize Context in Sessions

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/guidance/antipatterns.md

Avoid passing excessive context to sessions. Large contexts increase processing time, costs, and can introduce irrelevant information, confusing the model. Pass only the minimal, relevant context required for the task.

```prose
# Bad: Passing everything
let full_codebase = session "Read entire codebase"
let all_docs = session "Read all documentation"
let history = session "Get full git history"

session "Fix the typo in the README"
  context: [full_codebase, all_docs, history]  # Massive overkill

# Good: Minimal context
let readme = session "Read the README file"

session "Fix the typo in the README"
  context: readme
```

--------------------------------

### sessions

Source: https://github.com/openclaw/openclaw/blob/main/docs/cli/index.md

Lists stored conversation sessions.

```APIDOC
## sessions

### Description
Lists stored conversation sessions.

### Method
GET

### Endpoint
/sessions

### Parameters
#### Path Parameters
None

#### Query Parameters
- **json** (boolean) - Optional - Output in JSON format.
- **verbose** (boolean) - Optional - Include verbose session details.
- **store** (string) - Optional - Path to the session store.
- **active** (integer) - Optional - Filter sessions active within the last minutes.

### Request Example
```json
{
  "example": "openclaw sessions --active 60 --json"
}
```

### Response
#### Success Response (200)
- **sessions** (array) - A list of conversation sessions.
  - **id** (string) - The session's unique identifier.
  - **timestamp** (string) - The time the session was last active.
  - **details** (object) - (Optional) Verbose details about the session.

#### Response Example
```json
{
  "example": {
    "sessions": [
      {
        "id": "sess_abcdef12345",
        "timestamp": "2023-10-27T10:00:00Z"
      }
    ]
  }
}
```
```

--------------------------------

### Declare Mutable Variable with 'let'

Source: https://github.com/openclaw/openclaw/blob/main/extensions/open-prose/skills/prose/compiler.md

The 'let' keyword creates a mutable variable that can be reassigned. It captures the result of a session.

```prose
let research = session "Research the topic thoroughly"

# research now holds the output of that session
```
